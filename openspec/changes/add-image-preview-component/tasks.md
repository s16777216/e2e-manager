## 1. 依賴與元件準備

- [x] 1.1 安裝 `react-photo-view` 依賴套件。
- [ ] 1.2 在 `frontend/src/components/custom/` 目錄建立 `ImagePreview.tsx` 元件，封裝 `PhotoProvider` 與 `PhotoView` 邏輯。

## 2. 整合與實作

- [ ] 2.1 修改 `frontend/src/components/custom/StepAccordion.tsx`，將目前的圖片渲染 logic 整合進 `ImagePreview` 元件中。
- [ ] 2.2 調整 `StepAccordion.tsx` 的 CSS 樣式，確保點擊截圖時能正確觸發 `react-photo-view` 的預覽視窗。
- [ ] 2.3 驗證圖片放大、縮放、旋轉與關閉功能的正常運作。

## 3. 測試與驗證

- [ ] 3.1 手動測試 SSE 執行過程中圖片截圖的預覽效果。
- [ ] 3.2 確保深色模式下的 UI 風格符合專案設計語言。
